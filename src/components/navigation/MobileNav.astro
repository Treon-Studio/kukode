---
import Wrapper from '@/components/fundations/containers/Wrapper.astro';
import Button from '@/components/fundations/elements/Button.astro';
import Burger from '@/components/fundations/icons/Burger.astro';
import Logo from '@/components/global/Logo.astro';
import UserAvatar from '@/components/global/UserAvatar.astro';
import { getLangFromLocals, useTranslations } from '@/i18n/utils';
import { NAVIGATION_LINKS } from '@/lib/constants';

// Get user and profile from middleware
const user = (Astro.locals as any).user;
const profile = (Astro.locals as any).profile;
const isLoggedIn = !!user;

const lang = getLangFromLocals(Astro.locals);
const t = useTranslations(lang);

// Centralized navigation links mapped with translation helper
const links = NAVIGATION_LINKS.map((link) => ({
  href: link.href,
  text: t(link.translationKey),
}));
---

<nav class="fixed inset-x-0 top-0 z-40 backdrop-blur-md bg-white/80 border-b border-base-100 md:hidden">
  <Wrapper variant="standard" class="py-3">
    <div class="flex items-center gap-3">
      <Button
        iconOnly
        size="xs"
        variant="default"
        type="button"
        data-mobile-nav-toggle
        aria-expanded="false"
        aria-label="Open menu"
      >
        <Burger slot="icon" class="size-4" />
      </Button>
      <a href="/" aria-label="Go to homepage" class="group focus:outline-none">
        <Logo class="text-base-900" />
      </a>
    </div>
  </Wrapper>
  <div class="hidden md:hidden bg-white/95 border-b border-base-100 backdrop-blur-md" data-mobile-nav-panel>
    <Wrapper variant="standard" class="py-4">
      <div class="flex flex-col">
        {
          links.map((link) => (
            <a
              class="text-base-900 text-lg font-medium py-1 border-b border-base-100 last:border-b-0"
              href={link.href}
            >
              {link.text}
            </a>
          ))
        }
      </div>

      <!-- Language Selector -->
      <div class="mt-6 pt-6 border-t border-base-100 flex items-center justify-between gap-3 flex-wrap">
        <span class="text-xs font-semibold text-base-500 uppercase tracking-wider">Language / Bahasa</span>
        <div class="flex gap-2 flex-wrap">
          <a
            href="?lang=en"
            class={`px-3 py-1.5 rounded text-xs font-semibold border ${lang === 'en' ? 'bg-base-900 text-white border-base-900' : 'bg-white text-base-600 border-base-200'}`}
          >
            EN
          </a>
          <a
            href="?lang=id"
            class={`px-3 py-1.5 rounded text-xs font-semibold border ${lang === 'id' ? 'bg-base-900 text-white border-base-900' : 'bg-white text-base-600 border-base-200'}`}
          >
            ID
          </a>
          <a
            href="?lang=bjn"
            class={`px-3 py-1.5 rounded text-xs font-semibold border ${lang === 'bjn' ? 'bg-base-900 text-white border-base-900' : 'bg-white text-base-600 border-base-200'}`}
          >
            BJN
          </a>
          <a
            href="?lang=jv"
            class={`px-3 py-1.5 rounded text-xs font-semibold border ${lang === 'jv' ? 'bg-base-900 text-white border-base-900' : 'bg-white text-base-600 border-base-200'}`}
          >
            JV
          </a>
          <a
            href="?lang=ms"
            class={`px-3 py-1.5 rounded text-xs font-semibold border ${lang === 'ms' ? 'bg-base-900 text-white border-base-900' : 'bg-white text-base-600 border-base-200'}`}
          >
            MS
          </a>
        </div>
      </div>

      <!-- cmdk search trigger -->
      <div class="mt-6">
        <button
          type="button"
          data-cmdk-trigger
          aria-label={t('cmdk.open')}
          class="w-full flex items-center gap-3 px-4 py-2.5 bg-base-50 hover:bg-white border border-base-200 hover:border-accent-400 rounded-full text-sm text-base-500 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-accent-500/30"
        >
          <svg class="size-5 text-base-500 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          <span class="text-base-400 truncate text-left w-full">{t('cmdk.search')}</span>
          <kbd class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-mono font-medium text-base-500 border border-base-200 bg-white shrink-0">{t('cmdk.kbd')}</kbd>
        </button>
      </div>

      <div class="mt-4 flex items-center gap-2">
        {isLoggedIn ? (
          <>
            <div class="flex items-center gap-3 flex-1 min-w-0">
              <UserAvatar
                avatarUrl={profile?.avatar_url}
                username={profile?.username}
                email={user.email}
                class="size-8 shrink-0"
              />
              <span class="text-sm font-medium text-base-900 truncate">
                {profile?.full_name || profile?.username || 'User'}
              </span>
            </div>
            <Button isLink size="xs" variant="muted" href="/dashboard" class="shrink-0">
              {t('nav.dashboard')}
            </Button>
            <form method="POST" action="/api/auth/signout">
              <Button size="xs" variant="default" type="submit" class="shrink-0">
                {t('nav.signout')}
              </Button>
            </form>
          </>
        ) : (
          <Button isLink size="xs" variant="default" href="/signin" data-auth-trigger="signin" class="shrink-0 w-full"
            >{t('nav.signin')}</Button
          >
        )}
      </div>
    </Wrapper>
  </div>
</nav>

<script is:inline>
  (() => {
    const toggle = document.querySelector("[data-mobile-nav-toggle]");
    const panel = document.querySelector("[data-mobile-nav-panel]");
    if (!toggle || !panel) return;
    const close = () => {
      panel.classList.add("hidden");
      toggle.setAttribute("aria-expanded", "false");
    };
    const open = () => {
      panel.classList.remove("hidden");
      toggle.setAttribute("aria-expanded", "true");
    };
    toggle.addEventListener("click", () => {
      const isOpen = toggle.getAttribute("aria-expanded") === "true";
      if (isOpen) close();
      else open();
    });
    document.addEventListener("click", (e) => {
      if (!panel.contains(e.target) && !toggle.contains(e.target)) {
        close();
      }
    });
    panel.querySelectorAll("[data-auth-trigger]").forEach((el) => {
      el.addEventListener("click", () => {
        close();
      });
    });
  })();
</script>
