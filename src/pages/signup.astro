---
import Button from '@/components/fundations/elements/Button.astro';
import Text from '@/components/fundations/elements/Text.astro';
import BaseLayout from '@/layouts/BaseLayout.astro';

// Read error from URL params (set by API endpoint)
const errorMsg = Astro.url.searchParams.get('error');
---

<BaseLayout hideNav={true} hideFooter={true}>
  <div class="flex h-dvh">
    <div class="w-full max-w-md py-24 mx-auto my-auto">
      <div class="text-center">
        <Text
          tag="h1"
          variant="displayLG"
          class="text-base-900 font-display font-light"
        >
          Sign up
        </Text>
        <Text tag="p" variant="textBase" class="text-base-600 mt-4">
          Create your account below to start discovering and sharing maker tools on Kukode.
        </Text>
      </div>

      {/* Error message */}
      {errorMsg && (
        <div class="mt-6 rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-800">
          {errorMsg}
        </div>
      )}

      <form method="POST" action="/api/auth/signup" class="mt-10 space-y-4" id="signup-form">
        <div class="w-full">
          <div class="flex items-baseline justify-between mb-1">
            <label for="username" class="text-sm font-medium text-base-500">
              Username <span class="text-red-500">*</span>
            </label>
          </div>
          <div class="relative z-0 focus-within:z-10">
            <input
              id="username"
              name="username"
              type="text"
              required
              placeholder="e.g. janesmith"
              class="block w-full px-4 py-2 text-xs leading-tight align-middle bg-white border border-transparent transition duration-300 ease-in-out focus:z-10 h-9 rounded-md text-base-500 ring-1 ring-base-200 placeholder-base-400 focus:border-accent-500 focus:ring-accent-100 focus:ring-2 focus:outline-none shadow-sm"
            />
          </div>
        </div>

        <div class="w-full">
          <div class="flex items-baseline justify-between mb-1">
            <label for="full_name" class="text-sm font-medium text-base-500">
              Full Name
            </label>
          </div>
          <div class="relative z-0 focus-within:z-10">
            <input
              id="full_name"
              name="full_name"
              type="text"
              placeholder="e.g. Jane Smith"
              class="block w-full px-4 py-2 text-xs leading-tight align-middle bg-white border border-transparent transition duration-300 ease-in-out focus:z-10 h-9 rounded-md text-base-500 ring-1 ring-base-200 placeholder-base-400 focus:border-accent-500 focus:ring-accent-100 focus:ring-2 focus:outline-none shadow-sm"
            />
          </div>
        </div>

        <div class="w-full">
          <div class="flex items-baseline justify-between mb-1">
            <label for="email" class="text-sm font-medium text-base-500">
              Email Address <span class="text-red-500">*</span>
            </label>
          </div>
          <div class="relative z-0 focus-within:z-10">
            <input
              id="email"
              name="email"
              type="email"
              required
              placeholder="you@example.com"
              class="block w-full px-4 py-2 text-xs leading-tight align-middle bg-white border border-transparent transition duration-300 ease-in-out focus:z-10 h-9 rounded-md text-base-500 ring-1 ring-base-200 placeholder-base-400 focus:border-accent-500 focus:ring-accent-100 focus:ring-2 focus:outline-none shadow-sm"
              inputmode="email"
            />
          </div>
        </div>

        <div class="w-full">
          <div class="flex items-baseline justify-between mb-1">
            <label for="password" class="text-sm font-medium text-base-500">
              Password <span class="text-red-500">*</span>
            </label>
          </div>
          <div class="relative z-0 focus-within:z-10">
            <input
              required
              id="password"
              name="password"
              type="password"
              minlength="6"
              placeholder="••••••••"
              class="block w-full px-4 py-2 text-xs leading-tight align-middle bg-white border border-transparent transition duration-300 ease-in-out focus:z-10 h-9 rounded-md text-base-500 ring-1 ring-base-200 placeholder-base-400 focus:border-accent-500 focus:ring-accent-100 focus:ring-2 focus:outline-none shadow-sm"
              aria-required="true"
            />
          </div>
        </div>

        <div class="w-full">
          <div class="flex items-baseline justify-between mb-1">
            <label
              for="confirm-password"
              class="text-sm font-medium text-base-500"
            >
              Confirm Password <span class="text-red-500">*</span>
            </label>
          </div>
          <div class="relative z-0 focus-within:z-10">
            <input
              required
              id="confirm-password"
              name="confirm-password"
              type="password"
              minlength="6"
              placeholder="••••••••"
              class="block w-full px-4 py-2 text-xs leading-tight align-middle bg-white border border-transparent transition duration-300 ease-in-out focus:z-10 h-9 rounded-md text-base-500 ring-1 ring-base-200 placeholder-base-400 focus:border-accent-500 focus:ring-accent-100 focus:ring-2 focus:outline-none shadow-sm"
              aria-required="true"
            />
          </div>
        </div>

        <div id="local-error" class="hidden rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-800"></div>

        <div class="flex flex-col gap-2 pt-2">
          <Button size="base" variant="accent" type="submit" id="submit-btn">
            Create account
          </Button>
        </div>
      </form>

      <Text
        tag="p"
        variant="textXS"
        class="mt-6 font-medium text-center text-base-500"
      >
        Already have an account?
        <a
          href="/signin"
          class="font-medium text-accent-500 hover:text-base-500"
        >
          Sign in
        </a>
      </Text>
    </div>
  </div>
</BaseLayout>

<script is:inline>
  (() => {
    const form = document.getElementById('signup-form');
    const localErr = document.getElementById('local-error');

    if (form) {
      form.addEventListener('submit', (e) => {
        const password = document.getElementById('password').value;
        const confirmPassword = document.getElementById('confirm-password').value;

        if (password !== confirmPassword) {
          e.preventDefault();
          localErr.textContent = 'Password dan konfirmasi password tidak cocok.';
          localErr.classList.remove('hidden');
          return;
        }

        localErr.classList.add('hidden');
        const btn = document.getElementById('submit-btn');
        if (btn) {
          btn.setAttribute('disabled', 'true');
          btn.textContent = 'Creating Account...';
        }
      });
    }
  })();
</script>
